# websockets (arraybuffer)

> example use of the sync module websockets configured for arraybuffers

## Usage

In a terminal, run
```
npm run watch
```

open a browser that at: `http://127.0.0.1:3000`
